{
  "name": "Blossym",
  "version": "0.1.0",
  "description": "",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "client"
  ],
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "compile": "yarn run truffle compile",
    "start": "yarn run compile && yarn workspace client run start",
    "lint": "prettier --check .",
    "fix": "prettier --write .",
    "build": "yarn run compile && yarn workspace client run build",
    "test": "yarn workspace client run test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^3.3.0"
  },
  "devDependencies": {
    "husky": "^4.3.8",
    "lint-staged": "^10.5.3",
    "prettier": "2.2.1",
    "prettier-plugin-solidity": "^1.0.0-beta.3",
    "pretty-quick": "^3.1.0",
    "truffle": "^5.1.63"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,html,css,md,sol}": "prettier --write"
  }
}
